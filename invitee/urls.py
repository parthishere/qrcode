from django.urls import path
# from .views import qrscan, send_email, cam_feed, pati_gayela, bachya_khutya, home


app_name = "accounts"

urlpatterns = [
    # path('<str:unique_id>/update/', update_invitee, name='update'),
    # path("<str:unique_id>/delete/", delete_invitee, name='delete'),
    # path('<int:pk>/', detail_profile, name='detail-profile'),

]